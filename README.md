# MyFirstRespo1
numbers = [10, 12, 15, 8]
# Check if any number is greater than 14
result = any(num > 14 for num in numbers)
# result will be True because 15 is greater than 14
print(f"Condition check result: {result}")
